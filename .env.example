# ===== n8n =====
N8N_DEFAULT_LOCALE=zh-CN
N8N_PORT=5678
N8N_HOST=0.0.0.0
N8N_LISTEN_ADDRESS=0.0.0.0

# ===== Privacy / no-phone-home =====
N8N_DIAGNOSTICS_ENABLED=false
N8N_VERSION_NOTIFICATIONS_ENABLED=false
N8N_TEMPLATES_ENABLED=false
# Unset diagnostics configs / external hooks
EXTERNAL_FRONTEND_HOOKS_URLS=
N8N_DIAGNOSTICS_CONFIG_FRONTEND=
N8N_DIAGNOSTICS_CONFIG_BACKEND=

# ===== Task Runners =====
N8N_RUNNERS_ENABLED=true
N8N_RUNNERS_MODE=external
# sed -i "s/^N8N_RUNNERS_AUTH_TOKEN=.*/N8N_RUNNERS_AUTH_TOKEN=$(openssl rand -base64 36 | tr -d '\n')/" .env
N8N_RUNNERS_AUTH_TOKEN=change-this-to-a-real-secret
# Make broker reachable from other containers (n8n default is 127.0.0.1, which breaks n8n-runners)
N8N_RUNNERS_BROKER_LISTEN_ADDRESS=0.0.0.0
N8N_RUNNERS_BROKER_PORT=5679
# n8n-runners connects to n8n task broker websocket endpoint
N8N_RUNNERS_TASK_BROKER_URI=http://n8n:5679

# ===== n8n-runners custom image build args (optional) =====
# If you set these, they override docker-compose.yml defaults.
# RUNNERS_BASE_IMAGE=n8nio/runners:latest
# RUNNERS_JS_DEPS=moment uuid
# RUNNERS_PY_DEPS=numpy pandas
